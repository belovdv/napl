use crate::ast;
use crate::dfg;

// // To be done: this shouldn't be here.

pub fn ast22dfg(ast: Vec<ast::Line>) -> dfg::Space {
    let mut space = dfg::Space::new();

    for line in ast {
        
    }

    space
}
